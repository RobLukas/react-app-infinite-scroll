import styled from 'styled-components';
import CardDeck from 'react-bootstrap/CardDeck';

const CardDeckWrapper = styled(CardDeck)`
  padding: 15px 0;
`;

const PostsListStyles = { CardDeckWrapper };

export default PostsListStyles;
