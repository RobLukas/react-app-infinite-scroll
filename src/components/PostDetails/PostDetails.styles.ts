import styled from 'styled-components';
import Card from 'react-bootstrap/Card';

const CardWrapper = styled(Card)`
  padding: 10px;
`;

const PostDetailsStyles = { CardWrapper };

export default PostDetailsStyles;
